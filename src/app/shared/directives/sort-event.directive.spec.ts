import { SortEventDirective } from './sort-event.directive';

describe('SortEventDirective', () => {
  it('should create an instance', () => {
    const directive = new SortEventDirective();
    expect(directive).toBeTruthy();
  });
});
